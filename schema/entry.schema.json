{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://trustx.example/schema/entry.schema.json",
  "title": "AI Agent Risk Registry Entry (ARC 12-dimension model)",
  "description": "A single AI-agent risk-registry entry, scored against the ARC 12-dimension classification model in four groups (Autonomy & Decision Power; Action Authority & Reach; Persistence & Control; Data Authority & Confidentiality). Every dimension is ALWAYS scored 1/2/3; which dimensions drive the rolled-up risk_tier is a separate, per-system-type tier-weighting profile (policy/tier_weighting_profiles.yaml) recorded in tier_derivation. Dimension names, tier labels, and definitions are verbatim from the ARC reference (assets/arc_reference.json).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "agent",
    "dimensions",
    "risk_tier",
    "tier_derivation",
    "trust_level",
    "review",
    "provenance"
  ],
  "properties": {
    "agent": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "slug",
        "description"
      ],
      "properties": {
        "slug": {
          "type": "string",
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "description": "URL-safe identifier, e.g. 'invoice-payer'."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Free-text description of the agent that was classified."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Display title. Optional; adopted entries keep their published title, otherwise the slug is prettified for display."
        },
        "registry_id": {
          "type": "string",
          "minLength": 1,
          "description": "Upstream registry identifier (e.g. 'RAI-OPEN-0001') for entries adopted from a published reference registry."
        }
      }
    },
    "system_type": {
      "type": "string",
      "enum": [
        "knowledge_assistant",
        "code_content_generator",
        "decision_support_system",
        "tool_using_agent",
        "transaction_commerce_agent",
        "autonomous_agent",
        "embedded_physical_ai"
      ],
      "description": "ARC system type: knowledge_assistant = Knowledge assistant; code_content_generator = Code / content generator; decision_support_system = Decision support; tool_using_agent = Tool-using agent; transaction_commerce_agent = Transaction / commerce agent; autonomous_agent = Autonomous agent; embedded_physical_ai = Embedded / physical AI. Selects the tier-weighting profile; an entry without a system_type (or whose type has no profile) uses the default profile."
    },
    "autonomy_level": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "ARC autonomy level on the paper's L1-L5 scale, with the Section 2.4 verbatim definitions (Feng et al.'s five-level framework as quoted in arXiv:2607.09586; migrated from the earlier 1-3 scale — see ADR-0017). 1: Level 1 — Operator. The user directs all of the agent's actions, while the agent acts on command. 2: Level 2 — Collaborator. The user and the agent collaboratively plan and execute actions. 3: Level 3 — Consultant. The agent leads the actions, but consults the user for expertise and preferences. 4: Level 4 — Approver. The agent engages the user only in situations where they require a green-light in specified risk scenarios. 5: Level 5 — Observer. The agent has full autonomy, whereas the user only has the ability to monitor."
    },
    "dimensions": {
      "type": "object",
      "description": "The 12 ARC risk dimensions. ALL 12 are always scored (1/2/3), regardless of which subset the tier-weighting profile tiers off.",
      "additionalProperties": false,
      "required": [
        "autonomy",
        "decision_scope",
        "temporal_coupling",
        "action_authority",
        "system_reach",
        "blast_radius",
        "persistence",
        "reversibility",
        "control_authority",
        "data_sensitivity",
        "aggregation_risk",
        "data_egress_paths"
      ],
      "properties": {
        "autonomy": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Autonomy (Autonomy & Decision Power). 1 = Human-in-the-loop; 2 = Human-on-the-loop; 3 = Fully autonomous."
        },
        "decision_scope": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Decision Scope (Autonomy & Decision Power). 1 = Local decisions; 2 = Domain-level; 3 = Enterprise-wide."
        },
        "temporal_coupling": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Temporal Coupling (Autonomy & Decision Power). 1 = Isolated actions; 2 = Chained workflows; 3 = Continuous loops."
        },
        "action_authority": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Action Authority (Action Authority & Reach). 1 = Read-only / advisory; 2 = Create or modify; 3 = Execute transactions."
        },
        "system_reach": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "System Reach (Action Authority & Reach). 1 = Single system; 2 = Multiple internal; 3 = Cross-domain / third-party."
        },
        "blast_radius": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Blast Radius (Action Authority & Reach). 1 = Single user; 2 = Team / department; 3 = Enterprise / public."
        },
        "persistence": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Persistence (Persistence & Control). 1 = Stateless; 2 = Session-based; 3 = Long-term memory."
        },
        "reversibility": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Reversibility (Persistence & Control). 1 = Fully reversible; 2 = Partially reversible; 3 = Irreversible."
        },
        "control_authority": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Control Authority (Persistence & Control). 1 = Standalone; 2 = Supervises agents; 3 = Orchestrates fleets."
        },
        "data_sensitivity": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Data Sensitivity (Data Authority & Confidentiality). 1 = Public / non-sensitive; 2 = Internal / confidential; 3 = Regulated / crown-jewel."
        },
        "aggregation_risk": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Aggregation Risk (Data Authority & Confidentiality). 1 = No aggregation; 2 = Limited; 3 = Cross-session / inferential."
        },
        "data_egress_paths": {
          "allOf": [
            {
              "$ref": "#/$defs/dimension"
            }
          ],
          "description": "Data Egress Paths (Data Authority & Confidentiality). 1 = Constrained outputs; 2 = Multiple controlled; 3 = External egress."
        }
      }
    },
    "risk_tier": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high"
      ],
      "description": "Rolled-up risk tier (low/medium/high == Tier 1/2/3), computed by worst-case-wins over the tier-weighting profile's dimensions. Tier 1 — Low. All twelve dimensions at baseline. Standard documentation and acceptable-use controls. Tier 2 — Medium. Elevated dimensions but none at the highest level. Enhanced oversight, documented controls, periodic review. Tier 3 — High. At least one dimension at its highest level (worst-case-wins). Comprehensive governance: executive approval, real-time monitoring, kill-switch capability, pre-deployment testing."
    },
    "tier_derivation": {
      "type": "object",
      "description": "How the risk_tier was derived, so the derivation is inspectable: which tier-weighting profile applied and which dimensions actually drove the tier under worst-case-wins.",
      "additionalProperties": false,
      "required": [
        "profile",
        "tier_dimensions",
        "driving_dimensions"
      ],
      "properties": {
        "profile": {
          "type": "string",
          "description": "Name of the tier-weighting profile applied (policy/tier_weighting_profiles.yaml)."
        },
        "tier_dimensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "description": "The dimensions the profile tiers off (the weighting set)."
        },
        "driving_dimensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The tier_dimensions at the peak score — the ones that actually set the tier. Empty if the autonomy rule alone drove or lifted the tier."
        },
        "autonomy_level_driven": {
          "type": "boolean",
          "description": "True if autonomy level L5 forced Tier 3 under a profile with autonomy_rule 'arc_paper' (ARC paper Section 3.3; ADR-0017)."
        },
        "autonomy_level_lifted": {
          "type": "boolean",
          "description": "True if autonomy level L3/L4 lifted an otherwise-low tier to Tier 2 under a profile with autonomy_rule 'arc_paper' (ARC paper Section 3.3; ADR-0017). Distinct from autonomy_level_driven so autonomy-rule tiers stay distinguishable from dimension-driven ones."
        }
      }
    },
    "trust_level": {
      "type": "string",
      "enum": [
        "community-submitted",
        "working-group-reviewed",
        "verified"
      ],
      "description": "How much human scrutiny the ENTRY (its scoring and evidence) has earned — a separate axis from risk_tier (which is about the AGENT's risk). Earned, labelled, and revocable. community-submitted: passed the automated checks (schema, evidence, deterministic floors, recomputed tier, challenger) but no identified human has ratified it — the default a fresh contribution enters at. working-group-reviewed: an identified maintainer/working-group member has reviewed the scoring and evidence and ratified it. verified: reviewed AND traceable to authoritative source material (e.g. adopted from a published reference registry). An entry may NOT be 'verified' without a human reviewer recorded in review.reviewed_by (build_registry.py enforces this)."
    },
    "review": {
      "type": "object",
      "description": "Human-review metadata backing the trust_level, and the freshness horizon after which the entry must be re-ratified. Distinct from provenance (who submitted/approved the underlying classification): this records the working-group review that sets trust_level and when it lapses.",
      "additionalProperties": false,
      "required": [
        "reviewed_by",
        "reviewed_at",
        "expires_at"
      ],
      "properties": {
        "reviewed_by": {
          "type": [
            "string",
            "null"
          ],
          "description": "Identified maintainer / working-group member who reviewed the entry. Null for community-submitted entries (no human reviewer yet). MUST be non-null for trust_level 'verified' or 'working-group-reviewed'."
        },
        "reviewed_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "ISO-8601 timestamp of the review. Null until reviewed."
        },
        "expires_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "Freshness horizon: ISO-8601 timestamp after which the entry must be re-ratified. Null for entries that have never been reviewed."
        }
      }
    },
    "challenge": {
      "type": "object",
      "description": "Record of the challenger (red-team) pass over the proposal.",
      "additionalProperties": false,
      "required": [
        "flagged",
        "notes",
        "iterations"
      ],
      "properties": {
        "flagged": {
          "type": "boolean",
          "description": "True if the challenger flagged any unsupported score."
        },
        "notes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Human-readable challenge notes, including any deterministic overrides that fired."
        },
        "iterations": {
          "type": "integer",
          "minimum": 1,
          "description": "Number of proposer iterations that ran (bounded; max 2)."
        }
      }
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "submitted_by",
        "approved_by",
        "timestamp"
      ],
      "properties": {
        "submitted_by": {
          "type": "string",
          "description": "Who/what submitted the draft (e.g. 'classifier', an operator id, or 'RAI-ARC-reference' for adopted seed entries)."
        },
        "approved_by": {
          "type": [
            "string",
            "null"
          ],
          "description": "Accountable human who ratified the entry. Null until ratified."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "ISO-8601 timestamp of ratification."
        },
        "note": {
          "type": "string",
          "minLength": 1,
          "description": "Optional free-text provenance note, e.g. that a 'verified' entry is adopted from a published reference registry's reference material."
        }
      }
    }
  },
  "$defs": {
    "dimension": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "score",
        "rationale",
        "evidence"
      ],
      "properties": {
        "score": {
          "type": "integer",
          "minimum": 1,
          "maximum": 3,
          "description": "Tier score for this dimension: 1 (Tier 1 Low) to 3 (Tier 3 High)."
        },
        "rationale": {
          "type": "string",
          "minLength": 1,
          "description": "One-line justification for the score."
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Evidence pointers supporting the score (quotes from the description, links, doc refs). May be empty for a draft but SHOULD be populated for a defensible entry."
        }
      }
    }
  }
}
